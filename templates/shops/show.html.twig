{% extends 'base.html.twig' %}

{% block title %}Shops{% endblock %}

{% block body %}
    <h1>Shops</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shop.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shop.description }}</td>
            </tr>
            <tr>
                <th>Reviews</th>
                <td>{{ shop.reviews }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shops_index') }}">back to list</a>

    <a href="{{ path('shops_edit', {'id': shop.id}) }}">edit</a>

    {{ include('shops/_delete_form.html.twig') }}
{% endblock %}
